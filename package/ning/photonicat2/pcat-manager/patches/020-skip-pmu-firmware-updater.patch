--- a/meson.build
+++ b/meson.build
@@ -8,5 +8,4 @@ libusb1_deps = dependency('libusb-1.0')
 jsonc_deps = dependency('json-c')
 thread_deps = dependency('threads')
-zlib_deps = dependency('zlib')
 
 subdir('src')
--- a/src/meson.build
+++ b/src/meson.build
@@ -31,21 +31,3 @@ executable('fm350-mm',
     ['fm350-mm.c'],
     install: true
 )
-
-pcat_pmu_updater_sources = [
-    'pcat-pmu-updater.c'
-]
-
-pcat_pmu_updater_headers = [
-
-]
-
-executable('pcat-pmu-updater',
-    pcat_pmu_updater_sources,
-    pcat_pmu_updater_headers,
-    install: true,
-    dependencies : [
-        glib2_deps,
-        zlib_deps
-    ]
-)
\ No newline at end of file
